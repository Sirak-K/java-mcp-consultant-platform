{
  "catalog_id": "request_lifecycle_description_catalog",
  "runtime_contract_catalog_version": "1.0.0",
  "request_lifecycle_descriptions": [
    {
      "operating_surface": "MCP_DIRECT",
      "lifecycle_summary": "Direct MCP requests must authenticate first and then execute within exactly one active tenant context."
    },
    {
      "operating_surface": "APP_ADAPTER",
      "lifecycle_summary": "A future app adapter may start in pre-session but must bind to one tenant before business actions."
    },
    {
      "operating_surface": "PLATFORM_OPS",
      "lifecycle_summary": "Platform ops run on the shared core with platform identity by default and may only switch tenant via audited assume-tenant."
    }
  ]
}
