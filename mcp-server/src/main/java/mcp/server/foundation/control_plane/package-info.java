/**
 * Platform control plane persistence and status surfaces.
 *
 * This package is outside the request execution path and owns durable tracks for:
 * audit, usage, policy/config, and analytics.
 */
package mcp.server.foundation.control_plane;
