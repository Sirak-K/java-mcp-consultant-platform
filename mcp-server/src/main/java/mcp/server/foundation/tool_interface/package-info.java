/**
 * Foundation tool-interface boundary.
 *
 * <p>This package owns tool contracts, tool execution, and tool-facing
 * orchestration only. It must not become the canonical business service layer.
 */
package mcp.server.foundation.tool_interface;
