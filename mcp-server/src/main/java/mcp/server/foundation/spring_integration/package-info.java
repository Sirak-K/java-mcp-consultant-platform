/**
 * Foundation Spring-integration boundary.
 *
 * <p>This package owns wiring only: bean registration, runtime composition,
 * and configuration bridging. It must not become a second domain layer.
 */
package mcp.server.foundation.spring_integration;
