/**
 * Reference-data domain module.
 *
 * <p>
 * Owns stable lookup and classification data such as roles, skills, competency
 * levels and deterministic company identity lookup data. Passive catalogs are
 * exposed through resources; only parameterized lookup behavior belongs in
 * tools.
 */
package mcp.server.domain.reference_data;
